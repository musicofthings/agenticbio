id: SYNTH-ABS-002
title: Nextflow as the execution plane for agent plans
claim: DSL2 workflows remain the durable record when an agent is allowed only to propose `nextflow run`.
gap: Many “AI pipeline” demos still shell around the engine and lose resume state.
section: Approach (architecture, not wet-lab methods)
